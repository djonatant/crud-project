Tecnologias utilizadas:

 - Para o back-end
Framework Doctrine ORM.
Banco de dados MySql.

- Para o front-end
Framework jQuery (Para JS).
Framework Bootstrap (Para CSS).

COMO INSTALAR:

0.1 - Realizar git clone

1. Dentro da pasta Config > Config.php, ajustar o arquivo as constantes para o devido funcionamento do programa
1.1 APP_NAME > Nome da pasta que ficará hospedada o software.
1.2 Constantes com as iniciais 'DB', são referente as credenciais do banco de dados do sistema.

2. Após realizado o item 1, crie um banco de dados conforme definido na constante DB_DRIVER e após execute o arquivo cli-config.php, assim será criado os schemas necessários para o devido funcionamento do sistema.
